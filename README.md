
  # Build Frontend with Best UI/UX

  This is a code bundle for Build Frontend with Best UI/UX. The original project is available at https://www.figma.com/design/clNSGEA8DrQYFiX5mBWoys/Build-Frontend-with-Best-UI-UX.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  